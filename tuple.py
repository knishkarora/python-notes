# immutable, do not accept duplicate values, ordered, indexed, fixed size, heterogeneous data types

#tuple methods
my_tuple = (1, 2, 3, 4, 5) # Initial tuple
index_3 = my_tuple.index(3) # Finds the index of 3  
count_2 = my_tuple.count(2) # Counts occurrences of 2
sliced_tuple = my_tuple[1:4] # Slices the tuple from index
# 1 to 3 (4-1)
concatenated_tuple = my_tuple + (6, 7, 8) # Concatenates two tuples
repeated_tuple = my_tuple * 2 # Repeats the tuple twice
